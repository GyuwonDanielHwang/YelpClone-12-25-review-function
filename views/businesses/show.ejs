<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show</title>
</head>

<body>
    <h1>
        <%= business.title %>
    </h1>
    <h2>
        <%= business.location %>
    </h2>
            <p> <a href="/business/<%=business._id%>/edit">Edit</a>
            </p>
            <p>
            <form action="/business/<%=business._id%>?_method=DELETE" method="POST">
                <button>Delete</button>
            </form>
            </p>
            <footer>
                <a href="/business">All Businesses</a>
            </footer>

            <h2>Leave a Review</h2>
            <form action="/business/<%=business._id%>/review" method="POST" class="mb-3 validated-form" novalidate>
                <div class="mb-3">
                    <label class="form-label" for="rating">Rating</label>
                    <input class="form-range" type="range" min="1" max="5" name="review[rating]" id="rating" />
                </div>
                <div class="mb-3">
                    <label class="form-label" for="body">Review</label>
                    <textarea class="form-control" name="review[body]" id="body" cols="30" rows="3" required></textarea>
                    <div class="valid-feedback">Looks good!</div>
                </div>
                <button class="btn btn-success">Submit</button>
            </form>
            <% for(let review of business.reviews) { %>
                <div class="card mb-3">
                    <div class="card-body">
                        <h5 class="card-title">Rating: <%= review.rating %>
                        </h5>
                        <p class="card-text">Review: <%= review.body %>
                        </p>
                        <form action="/business/<%=business._id%>/review/<%=review._id%>?_method=DELETE" method="POST">
                            <button class="btn btn-sm btn-danger">Delete</button>
                        </form>
                    </div>
                </div>
                <p> 
                    <a href="/business/<%=business._id%>/reviewedit">Edit</a>
                </p>
            <% } %>
</body>

</html>